extends layout

block content
    h1= title
    p Welcome to #{title}

    div(id='pinContainer')
        div(id='slideContainer')
            each val in colors
                section(class='panel')
                    h2= val
    script(src='/javascripts/colorScroll.js')